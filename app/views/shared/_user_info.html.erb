<a href="<%= user_path(current_user) %>">
<%= link_to image_tag("anonymous.png"),edit_user_path(current_user) if !current_user.image? %>
  <%= image_tag current_user.image_url(:thumb) if current_user.image?%>
</a>
<h1>
<%= current_user.name %>
</h1>
<span>
<%= link_to "view my profile", current_user %>
</span>


<span>
<%= pluralize(current_user.questions.count, "question") %>
</span>
